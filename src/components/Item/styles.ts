import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        borderBottomWidth: 1,
        borderBottomColor: "#E4E6EC",
        flexDirection: 'row',
        gap: 8,
        paddingBottom: 8,
        paddingTop: 8
    },
    text: {
        fontSize: 14,
        color: "#333",
    },
    clearButton: {
        marginLeft: 'auto',
        color: "#828282"
    },
})